# Gritive Plugin

코드베이스 종합 리뷰, 클린까지 반복하는 리뷰 루프, 페르소나 기반 UX 테스트, RFP 기반 프로젝트
부트스트랩 플러그인.

## Structure

- `skills/codebase-review/` — 리뷰 오케스트레이션 스킬. 리뷰어 3개(backend, frontend, security)는
  `reviewers/*.md` 하위 문서다 — **에이전트가 아니다**(아래 Reviewer Contract). **개념이 아니라
  계층으로 나뉜다** — 근거는 `codebase-review/SKILL.md`의 도메인 표에 있다
- `skills/review-forever/` — 리뷰 스킬을 감싸 **실행 큐가 빌 때까지** 반복하는 루프 (자체 리뷰 로직 없음)
- `skills/persona-test/` — 페르소나 기반 서비스 테스트 스킬
- `skills/project/` — RFP → PRD·이슈 부트스트랩 + 백로그 자율 처리 (6개 서브커맨드 라우터: setup/prd-to-issue/sync/gap/build/loop).
  `build`는 두 파일이다: `build.md`(오케스트레이터 — 선정·이슈 생성·**리뷰·PR**·머지)와
  `build-issue.md`(subagent — split·Coverage Plan·구현·검증·Audit). 긴 세션에서 지침이 유실되므로
  이슈마다 fresh subagent에 위임한다 — 아래 "긴 세션에서의 지침 유실" 참조.
  **9.5단계의 목적은 PR이 아니라 리뷰다.** 어느 모드든 **PR 전에 리뷰가 반드시 한 겹 돈다** —
  무엇이 도는지의 정본은 `build.md`의 "리뷰 게이트" 절이다. 여기 옮겨 적지 않는다.
  실측된 실패 양식만 남긴다: 리뷰를 안 부르고 `gh pr create`부터 치면 그 한 겹이 통째로 사라진다
  (baseline 3/3). 그래서 9.5단계는 **리뷰 → 판정 → PR** 순서로 적혀 있다 — 순서를 뒤집거나
  "커밋이 끝났으니 PR만" 으로 요약하지 마라.
  **10단계(머지·배포)에는 리뷰가 없다 — 백스톱도 두지 않는다.** 백스톱을 두면 에이전트가 그 자리를
  구멍으로 읽고 리뷰를 한 번 더 돌린다(실측 1/2). 겹은 하나뿐이고 그 하나가 9.5단계다.
  **인터랙션 규약의 정본은 `setup.md` 5단계 하나다** — `sync.md` 4단계는 그 표를 복제하지 않고 읽어서
  쓴다. `build-issue.md`·`gap.md`는 규약 절의 **존재**를 조건으로 삼으므로, 조건이 거짓일 때 조용히
  넘어가지 않고 "`/project sync` 필요"를 보고하는 분기를 반드시 유지한다 — 그 분기가 없으면 규약이
  없는 프로젝트에서 Storybook 정본 story backfill이 영영 발동하지 않는다(실측으로 재현됨)
- `scripts/push.sh` — 버전 bump + push (`git release` alias)
- `.claude-plugin/` — Claude Code용 `plugin.json` + `marketplace.json`
- `.codex-plugin/` — Codex용 `plugin.json`
- `.agents/plugins/marketplace.json` — Codex용 marketplace 등록 정보
- 버전은 `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.codex-plugin/plugin.json`에
  따로 있으므로 항상 함께 bump

`skills/project/`는 `~/.claude/skills/prototype`(user space)에서 가져온 사본이다. 원본은 그대로
남아 있으므로 **양쪽이 갈라진다.** 한쪽만 고치고 동기화됐다고 가정하지 마라.

`skills/review-forever/`는 `~/.claude/skills/review-forever`(user space 스킬. gstack 트리 안에 있지
않다)에서 가져왔지만 **사본이 아니다.** codex/agy 폴백을 걷어냈고, 원본에 없던 수렴 안전장치(패스
상한, 실행 큐 기반 정체 감지, 발견 동일성 키, "중단 ≠ 발견 0건" 구분)를 넣었다. 원본과 동기화할
대상이 아니다.

**모든 판정은 원시 발견 수가 아니라 실행 큐로 잰다** — `발견 − 범위 밖 레거시 − 기각한 오탐 − 보류`.
감싼 리뷰 스킬은 패스 간 기억이 없어서 고치지 않기로 한 것을 매 패스 다시 보고하므로, 원시 발견 수로
완료·정체를 재면 루프가 지시를 완벽히 따르고도 영영 끝나지 않는다. 이 스킬을 고칠 때 이 불변식을
깨지 마라 — 리뷰 3회에 걸쳐 이 자리에서만 CRITICAL이 네 번 나왔다.

**이름이 원본과 충돌한다 — 접두사가 유일한 방어선이다.** 이 스킬은 `gritive:review-forever`이고,
접두사 없는 `/review-forever`는 **원본**(안전장치 없음)으로 간다. `prototype`을 `project`로 개명해
충돌을 피했던 것과 달리 여기서는 이름을 유지했으므로, **문서·안내·자기 참조에서 접두사를 절대
빠뜨리면 안 된다.** 빠뜨리면 사용자는 우리가 약속한 안전장치가 없는 루프를 돌리게 된다.

**기본 리뷰 스킬은 gstack의 `review`다.** personal 스킬이 bundled를 덮어쓰므로 `review`는 Claude Code
내장 review가 아니라 gstack 것으로 해석된다. 이는 의도된 선택이다 — 착각이 아니라 결정이므로
"내장 review로 고쳐야 한다"고 되돌리지 마라.

## 스킬 경계

**`review-forever`는 코드를 고친다.** 리뷰어가 읽기 전용인 것과 루프가 수정하는 것은 층이 다르다:
**에이전트가 찾고, 루프가 고친다.**

**`persona-test`는 코드를 고치지 않는다** — 발견을 `gap`과 똑같이 이슈로 등록한다(이슈 관리 방식이
있으면; 없으면 리포트로 폴백). `plugin` 인터페이스가 hook을 띄우려고 대상 파일을 고치는 것은 테스트
조작이므로 허용하되 원복한다.

**`persona-test`의 스크린샷·리포트는 저장소 밖 고정 경로에 자동 보존한다.** 사라지는 임시 디렉토리에
두면 묻지 못하는 실행(subagent)에서 근거가 통째로 유실된다. 경로의 정본은 `persona-test/SKILL.md`의
"산출물 디렉토리" 절이다 — 여기 옮겨 적지 않는다. `.persona-test/` 같은 **저장소 안** 디렉토리로
되돌리지 마라(2026-08-04 판단).

`project loop`는 자체 로직 없이 `build`→`gap`→`build`→`persona-test`→`build`를 **새 빌드가능 이슈가
안 나올 때까지** 반복하는 오케스트레이터다. `review-forever`와 같은 수렴 불변식을 쓴다: **완료·정체를
원시 발견 수가 아니라 "새로 만든 빌드가능 이슈"(= gap+persona 이슈 − 중복 − 제외 클래스)로 잰다.**
제외 클래스 목록의 정본은 `loop.md`의 "빌드 제외 클래스" 절이다 — **여기 옮겨 적지 않는다.** 실제로
목록이 늘 때마다(`build-blocked` 추가) 이 자리가 낡았다. 이 불변식을 깨면 gap/persona가 매 라운드 같은 것을 다시 발견해
루프가 영영 끝나지 않는다. build가 중단 조건으로 멈추면 gap/persona로 넘어가지 말고 **루프도 멈춘다**
(중단 ≠ 수렴). persona가 서비스 기동 실패로 안 돈 라운드의 "발견 0"은 **수렴 신호가 아니다.**
**`loop --demo`는 build 호출과 큐 회계 양쪽에 같은 범위를 적용한다 — 한쪽만 좁히면 수렴이 불가능해진다.**
"데모 범위 밖"은 불변식의 예외가 아니라 **제외 클래스의 한 항목**이다: 실행 큐는 원래부터 "build가 실제로
소비하는 이슈"만 세고, `loop.md`의 제외 클래스 목록이 그 정의를 풀어 쓴 것이다. build에만
붙이면 build가 영영 안 태울 이슈를 큐가 계속 세어 라운드 상한까지 돌고 "정체"로 끝난다.
**범위 밖 발견은 버리지 않는다** — gap·persona는 전 범위를 그대로 보고 이슈로 남기며, 좁히는 것은
**소비**이지 발견이 아니다. 쌓인 이슈 수는 종료 보고에 반드시 적는다. 안 적으면 데모 수렴이 전체
수렴으로 읽힌다.

**`--demo`는 범위·깊이·리뷰 경로·발견 처분 네 축을 바꾼다.** 축 목록과 각 축의 정본은 `build.md`의
`--demo` 절과 "리뷰 게이트" 절이다 — 여기 값을 옮겨 적지 않는다. 목적은 수주 전에 데모를 빨리 보여주는 것이다.

**바꾸지 않는 것이 이 설계의 전부다:**

- **리뷰가 PR 전에 한 겹 도는 것 자체.** 바뀌는 것은 **무엇을 돌리느냐**이지 리뷰의 유무가 아니다
  (정본은 `build.md`의 "리뷰 게이트" 절). 느린 원인은 리뷰가 아니라 **발견을 전부 고치는 루프**이므로
  줄이는 것은 수정이지 리뷰가 아니다.
  게다가 리뷰가 `demo-debt` 목록의 **재료**다 — 한 겹마저 빼면 무엇을 미뤘는지조차 안 남는다.
- **`Closes` 100%와 Coverage Audit.** happy path만 구현한 Req가 `done`을 얻는 조건은 **미룬 것이
  `demo-debt` 이슈로 공개됐다는 것**이다 — 자격 증명 fail-fast·리서치 기본값이 `done`을 얻는 방식과
  같은 모양이고, 그래서 새 개념이 아니라 세 번째 분리 항목이다. 공개 없이 얇게 구현한 것은 `done`이 아니다.
- **검증(7단계)과 게이트 무르는 답의 금지.** 검증 실패는 리뷰 발견이 아니라 **데모가 안 돈다는 뜻**이다.
- **실제 시크릿 값 노출은 즉시 중단.** 미루려면 이슈 본문에 그 값을 적어야 하므로 유출이 한 겹 늘어난다.

**`--demo`에서 유일한 진짜 실패는 미룬 것을 이슈로 안 남기는 것이다.** 그건 데모가 아니라 은폐된 부분
구현이고, 수주 후 백로그로 돌아올 유일한 경로가 그 이슈다.

**`persona-test`의 발견만 `demo-debt`에 적혀 있어도 고친다 — `gap`은 중복 처리한다.** 셋의 층이 다르다:
리뷰는 코드를 읽고 낸 예측, **gap도 문서 대비 코드의 정적 분석이라 같은 층**, persona만 **실제로 서비스를
돌려 드러났다는 관측**이다. gap까지 고치게 하면 안 되는 이유는 `--demo`가 미루는 것(검증·에러 처리·
접근성·i18n)이 정확히 gap이 PRD·design-guide를 근거로 매 라운드 다시 찾아내는 부류라,
**`--demo`가 자기가 미룬 것을 그대로 다시 구현하고 큐가 영영 안 빈다**는 것이다.

수렴 근거가 둘로 갈린다: persona는 **소진**(고쳐서 닫히면 재발견 없음), gap은 **중복 제거**(이미 부채로
기록됨). 어느 쪽도 발견을 버리지 않는다. `demo-debt` 라벨 이슈를 큐 제외 클래스에 넣는 것과 혼동하지
마라 — 라벨 제외는 build가 그 이슈를 안 태운다는 뜻이고, gap 중복 판정은 **내용 대조**다.

**리뷰 부채 `demo-debt` 이슈는 자동으로 닫지 않는다.** 그 안에 미수정 CRITICAL/HIGH·보안 발견이 들어
있어서, 기능 하나를 고친 것이 목록 전체를 갚았다는 뜻이 아니다. 구현 부채만 닫는다.

**PRD를 못 찾으면 `--demo`는 멈춘다 — 일반 build로 넘어가지 않는다.** build에 이슈 상한이 없어서
넘어가면 **"데모 하나 보자"고 친 명령이 백로그 전체를 태우고 자동 배포**한다. 사후 보고로는 못 막는다.
`loop`도 그 라운드에 멈춘다 — 이어 돌리면 `--demo`를 친 사용자가 전 범위 자율 실행을 받는다.

**persona 이슈에는 `출처: persona-test` 마커가 반드시 들어간다.** `build-issue.md`의 "관측된 것은
안 미룬다" 규칙이 이 한 줄에만 매달려 있다. 마커를 지우면 규칙이 조용히 죽고 데모에서 실제로 깨진 것이
다시 부채로 미뤄진다(리뷰가 "precondition을 아무도 안 만든다"로 잡았다). persona의 동일성 키 중복 검사는
`demo-debt`를 대조 대상에서 뺀다 — 안 그러면 관측된 파손이 "이미 부채에 있음"으로 접힌다.

**`loop.md`는 반환 계약 표를 subagent에 그대로 붙여 넘긴다.** `Ng`/`Np`는 `gap.md`·`persona-test`에
없는 개념이라, 안 넘기면 원시 이슈 수를 `Ng`로 쓰거나 없는 값을 0으로 읽어 거짓 수렴한다.
`build.md` 3.5단계가 같은 이유로 같은 규칙을 둔다 — 한쪽만 고치지 마라.

### 본문 규칙을 고치면 요약 자리를 같은 커밋에서 전부 고쳐라

**이 저장소에서 가장 자주 재발한 결함이다 — 리뷰 4라운드 중 3라운드에서 나왔고, 매번 CRITICAL이었다.**
정책을 고치면 그 정책을 **요약해 둔 자리**가 조용히 낡는다. 요약 자리는 셋이다:

- **Red Flags 절** (`loop.md`) — **에이전트가 판단 직전에 보고 금지형으로 적혀 있어 본문보다 강하게 읽힌다.**
- **Rationalization Table** (`build.md`·`build-issue.md`)
- **usage 블록·README·`SKILL.md` 요약**

**고칠 때 grep으로 확인하라** — 바꾼 정책의 핵심 단어(`demo-debt`, `CRITICAL/HIGH` 등)로 전 문서를
훑어 사본이 남았는지 본다.

**요약 자리는 값을 복사하지 말고 정본을 가리켜라.** 심각도 목록·조건식·판정 기준을 요약에 적는 순간
그 자리는 다음 개정에서 반드시 틀린다.

**`--demo`는 배포 대상을 제한하지 않는다 — 의도된 선택이다.** 리뷰가 "미수정 결함을 production에
자동 배포한다"고 잡았고, 사용자가 제약을 걸지 않기로 결정했다. 데모를 어디에 띄울지는 사용자가 관리한다.
"구멍이니 격리 경계를 넣자"로 되돌리지 마라 — 판단이 필요하면 다시 물어라.

**배포 명령은 대상 프로젝트 `CLAUDE.md`의 `## Deploy` 절에서 읽어 그대로 실행한다 — 이것도 의도된
선택이다.** 리뷰가 "저장소 write 권한이 곧 자율 루프 실행 머신에서의 임의 셸 실행"이라고 잡았고
(그 파일은 이 파이프라인이 사람 승인 없이 머지하는 저장소 안에 있다), 사용자가 **실행하기로 결정했다.**
allowlist·형태 검사·실행 전 확인·"이번 diff가 `## Deploy`를 건드렸으면 배포 중단" 같은 제약을 넣지
마라. 실행한 명령은 완료 보고에 남으므로(`build.md` 10단계·완료 보고) 사후 확인 경로는 이미 있다.
이 판단이 다시 필요해지면 되돌리지 말고 물어라.

**`--demo`는 mock을 허용하지 않는다** — 얇게 만드는 것은 경로의 **폭**이지 완성도가 아니다.
git log에 같은 이름의 옛 `--demo`가 있고 그것은 mock 인증 규약·격리 경계를 달고 있었다. 되살리지 마라.

### 이 파일은 배포되지 않는다

**플러그인 루트의 CLAUDE.md는 사용자 프로젝트에 컨텍스트로 로드되지 않는다.** 플러그인은
skills / agents / hooks 로만 컨텍스트를 기여하고, `plugin.json`에는 공통 지시문을 싣는 필드가 없다
([공식 문서](https://code.claude.com/docs/en/plugins-reference#file-locations-reference)).

즉 여기 쓴 규칙은 **이 저장소를 개발할 때만** 유효하다. 런타임에 실제로 강제하려면 규칙을 각
`skills/*/SKILL.md`(또는 그 하위 지침 문서)에 **복제해야 한다.** 새 공통 규칙을 만들 때는 반드시
배포되는 문서에도 넣어라 — 여기에만 쓰면 아무 효력이 없다. 반대로, 공통 규칙을 만들기 전에 **각
스킬 문서가 자기 동작을 적는 것으로 충분한지** 먼저 봐라. 복제 비용이 곧 낡는 사본이 된다.

## Reviewer Contract

리뷰어는 **플러그인 에이전트가 아니라 `skills/codebase-review/reviewers/*.md` 문서다.**
`codebase-review` Phase 3이 읽기 전용 subagent를 dispatch하고 그 문서 경로를 넘기면, subagent가
Read해서 그대로 수행한다 — `project`가 `build-issue.md`를 넘기는 것과 같은 방식이다.

**`agents/`로 되돌리지 마라.** 플러그인 에이전트는 Claude Code 전용인데 `.codex-plugin/plugin.json`은
`skills`만 싣는다 — 에이전트로 두면 Codex 배포본에서 `codebase-review`가 dispatch 단계에서 죽는다.
문서로 두면 양쪽에서 돈다. 대가로 `effort`·`tools` frontmatter 제어를 잃었다: 리뷰 깊이는 세션
effort를 따르고, 읽기 전용은 `subagent_type`을 `Explore`(Edit/Write 없음)로 잡아 확보한다.

리뷰어 공통 규칙:

- 모든 에이전트는 프롬프트에서 `mode` (base-diff/full), `scope` (backend/frontend/all),
  `base` (git diff에 넘길 리뷰 기준 인자), `files` (대상 파일 목록, base-diff일 때만)를 받는다
- `files`는 **파일 목록이지 diff가 아니다.** 변경 내용이 필요하면 `git diff {base} -- {files}`로 본다.
  리뷰 단위는 '변경된 파일' 전체이지 '변경된 라인'이 아니다
- **기본은 `base-diff`다.** 전체 코드베이스 스캔은 `mode=full`일 때만 한다
- `base-diff`에서 발견 사항은 `files` 안에 위치해야 한다. 문맥 파악을 위해 코드베이스 전체를
  읽는 것은 허용된다 — **읽는 범위 ≠ 보고 범위**
- `files`가 비면 "대상 없음"으로 종료한다. **전체 스캔으로 폴백하지 않는다**
- `scope`는 스킬이 `files`를 필터링할 때만 쓴다. 리뷰어가 `scope`를 하드 게이트로 재검사하면
  `--domain` 강제 호출과 충돌하므로 그렇게 하지 않는다
- 시작 시 반드시 프로젝트의 CLAUDE.md를 읽고 규칙을 반영한다
- 심각도 체계: CRITICAL / HIGH / MEDIUM / LOW
- 코드 수정은 하지 않는다 — 발견 사항만 보고한다
- **대상 프로젝트에 파일을 쓰지 않는다** — 리포트는 응답 텍스트로만 돌려준다. `Explore`는 Edit/Write가
  없지만 `Bash`는 있으므로 "코드 수정 금지"만으로는 리포트 파일 쓰기가 막히지 않는다
- 출력은 구조화된 마크다운 테이블 형식이다

전역 그래프가 필요한 항목(순환 의존, 번들 크기, 미사용 의존성, 고아 파일 전수조사 등)은
각 리뷰어가 `base-diff` 모드에서의 축소 규칙을 자기 파일에 정의한다.

**계약의 유일한 예외**: **데드코드 렌즈를 가진 리뷰어**(`backend`/`frontend`)는 이 변경이 마지막 호출을 제거해 고아가 된 심볼을
`files` 밖이어도 보고한다 (근거 명시 필수, **1홉까지만**). SKILL.md의 Phase 4 검증도 이 예외를
살려둔다 — 그러지 않으면 예외가 리포트 단계에서 죽는다.

## Development

리뷰어 추가/수정 시:

- `skills/codebase-review/reviewers/`에 `.md` 파일 생성. **frontmatter는 넣지 않는다** — 스킬이
  아니라 dispatch된 subagent가 Read하는 문서다
- 문서 머리에 "이 문서는 스킬이 아니라 dispatch된 subagent가 Read해서 수행하는 지침"이라고 적는다.
  안 적으면 다음 사람이 스킬로 착각해 frontmatter를 붙인다
- **`SKILL.md` Phase 3의 도메인별 지시 목록과 게이팅 표에 함께 추가한다.** 문서만 만들면 아무도
  dispatch하지 않는다
- 스킬 문서를 고쳐도 **실행 중인 세션에는 반영되지 않는다.** 세션은 설치된 사본
  (`~/.claude/plugins/cache/`)을 쓴다. 실제 동작 확인은 플러그인 업데이트 후에 해야 한다
- 공통 인터페이스 계약 준수 (mode/scope/base/files, CLAUDE.md 로딩, 심각도 통일, 읽기 전용, 테이블 출력)
- 전역 스캔이 필요한 점검 항목이 있으면 `base-diff` 모드에서의 축소 규칙을 반드시 명시
- `codebase-review/SKILL.md`의 도메인 테이블에 추가

## 문서가 커지는 이유와 줄이는 법

스킬 문서는 **정책축 × 절축의 곱**으로 부푼다. 정책 하나를 추가하면 원칙·전용 절·해당 단계·중단
조건·완료 보고·Rationalization Table에 각각 한 번씩 들어가 한 줄이 아니라 **한 열**이 늘어난다.
실측: 압축 전 `build.md`에서 자격 증명·고객 협의 정책이 9개 절에 47회 재진술됐다.

세 가지 규칙으로 막는다:

- **정책마다 정본 절을 하나 정하고 다른 절은 그것을 가리킨다.** 인터랙션 규약(`setup.md` 5단계),
  자격 증명·고객 협의(오케스트레이터 쪽은 `build.md`의 전용 절, 전문은 `build-issue.md`),
  보안·리뷰 판정(`build.md`의 "리뷰 게이트" 절)이 그 예다. **정본 절의 이득은 단어 수가 아니라 재팽창
  방지다** — 순감은 작지만 고칠 자리가 하나로 준다. 다만 정본 절 자체를 설명으로 부풀리지 마라: 규칙만
  적고 왜 그런지는 여기 CLAUDE.md에 한 줄로 남긴다.
- **중단 조건·검사 목록은 그것을 직접 관측하는 액터의 문서에만 둔다.** 오케스트레이터는 subagent가
  `stopped`를 돌려준 것을 볼 뿐 그 조건을 직접 재지 않으므로, 목록을 복제하면 죽은 사본이 된다.
- **평행 구조 블록은 공통 절차 + 차이 표로 접는다.** `build.md` 9단계의 사람 작업 이슈 세 종류
  (자격 증명·스펙 결정·Hollow 추적)가 그 사례다(392→250단어, 동작 회귀 없음). 이슈 본문 필드·라벨의
  정본은 그 표이고, `build-issue.md`는 subagent가 **무엇을 반환할지**만 정한다.

**문서 동기화·VERSION·CHANGELOG는 `build`의 게이트가 아니다 — 의도적으로 뺐다.** 언급 자체를 남기지
않았다. 문서·버전 부채는 되돌릴 수 있고 배포 위험이 아니다. 리뷰가 "정책이 사라졌다"고 잡거든 이 줄을
근거로 기각하라 — 실제로 한 라운드를 여기에 썼다.

**줄이기 전에 반드시 baseline을 잰다.** 장황함의 일부는 실측으로 얻어낸 것이고 일부는 그냥 쌓인
것인데, 문서만 봐서는 구분되지 않는다. 시나리오를 subagent에 돌려 준수율을 재고, 압축 후 같은
시나리오로 회귀를 확인한다 — 이것이 writing-skills의 REFACTOR 국면이다.

## subagent 위임의 하드 제약 — 사용자 질문과 깊이 예산

**subagent는 subagent를 띄울 수 있다.** 2026-07-28 실측(Claude Code 2.1.220): main → L1 → L2 → L3까지
Agent dispatch가 전부 성공했다. 문서도 같은 말을 한다 — 기본 깊이 상한은 main 아래 **3겹**이고 상한에
닿은 subagent에서만 Agent 툴이 회수된다([sub-agents](https://code.claude.com/docs/en/sub-agents#let-subagents-spawn-their-own-subagents)).
상한은 `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`로 **더 낮출 수 있다**(v2.1.217+).

**위임을 막는 진짜 제약은 둘이다:**

1. **subagent에는 `AskUserQuestion`이 없다**(실측: Claude Code 2.1.220, L1). 깊이별로 따로 재지는
   않았으므로 "모든 깊이에서"라고 단정하지 마라 — 필요한 결론에는 L1만으로 충분하다. 위임한 문서의
   질문은 **사용자에게 도달하지 않는다.**
2. **깊이 예산은 유한하고 사용자가 더 줄일 수 있다.** 3겹이 성립하는 것에 **의존하는 설계를 하지 마라** —
   상한을 1로 잡은 사용자에게서 조용히 죽는다.

**판정 기준은 "묻는가"가 아니라 "그 질문이 게이트인가"다.** 묻는 문서는 흔하다. 갈리는 것은 답을
못 받았을 때다:

- **게이트 질문** — 답이 없으면 진행하면 안 되는 질문. 위임하면 사용자에게 닿지 않고, 그 다음은
  둘 중 하나다: 호출자가 답을 지어내거나, 조용히 넘어가거나. **어느 쪽이든 게이트는 꺼진다.**
- **강등 가능한 질문** — 답이 없을 때 **문서에 명시된 열화 경로**가 있는 질문(관례 경로 시도,
  파일 대신 대화 리포트). 위임해도 게이트가 죽지 않는다. 단 열화 경로가 **문서에 적혀 있어야**
  한다 — 없으면 그건 강등이 아니라 즉흥이다.

그래서 현재 이 저장소의 문서는 **게이트 질문 때문에 위임 못 하는 것이 하나도 없다**:

- `build.md`·`build-issue.md`는 **묻지 않는다.** 모든 판정이 규칙이고, 조건이 깨지면 묻는 대신 멈추고
  사유를 반환한다.
- **`gap.md`·`persona-test`는 묻지만 그 질문이 전부 강등 가능하다.**
  근거 문서 경로(`gap.md` 1단계)는 관례 경로로, 대상 저장소 안에 리포트를 남길지(`gap.md` 폴백)는
  "묻지 못하면 저장하지 않는다"로, 인터페이스 선택(`persona-test`)은 감지된 것 전부로
  강등된다. **이 열화 경로들이 두 문서에 명시돼 있어야 이 분류가 성립한다** — 지우지 마라.
  **`persona-test`의 산출물 보존은 애초에 묻지 않는다** — 저장소 밖 고정 경로라 질문이 없고, 따라서
  강등도 없다. 이걸 다시 "물어보고 저장"으로 되돌리면 subagent 실행에서 근거가 통째로 유실된다.
  강등한 사실은 반환 계약의 `degraded` 항목으로 호출자에게 올린다(`loop.md` Phase B·C).

### 위임을 막는 두 번째 제약 — 관측성

**`loop`은 그래도 `build`를 위임하지 않는다.** 게이트 질문 때문이 아니라 **사용자가 아무것도 못 보게
되기 때문이다.** subagent의 진행은 호출자에게도 사용자에게도 중간에 안 보이는데, build는 백로그를
끝까지 드레인하므로 그 한 번이 매우 길다 — 이슈 선정·리뷰·PR·머지가 통째로 사라진다.

**두 근거의 세기가 다르다는 것을 기억하라.** `build` → `build-issue` 위임은 **실측**이고(581B→72B),
`loop` → `build` 위임은 "loop 세션이 가장 오래 산다"는 **유추**였다. 측정된 이득이 없는 쪽이 관측성을
가져갔으므로 되돌렸다. 컨텍스트 절감만 보고 다시 위임하지 마라.

(진행 보고를 `TaskCreate`/`TaskUpdate`로 올리면 깊이와 무관하게 사용자에게 보이지만, 그건 Claude Code
harness 기능이라 Codex 배포본에는 없을 수 있다. 인라인이 더 단순해서 그쪽을 골랐다.)

**깊이는 L1까지만 쓴다.** `loop`(main) → `build-issue` 또는 리뷰어 subagent(L1). `build`와 리뷰 스킬은
`Skill` 툴 인라인 호출이라 겹을 먹지 않는다. 여유가 많지만 **그 여유를 설계에 쓰지 마라** — 사용자가
상한을 낮출 수 있다.

**위임을 검토할 때 컨텍스트 절감만 계산하지 마라. 먼저 "이 문서의 질문이 게이트인가"를 확인하라.**
이 함정에 한 번 빠져 릴리스까지 간 적이 있다 — 리뷰를 살리려고 만든 PR 단계를 위임했더니 그 단계가
리뷰 게이트를 껐다. **지금 게이트 질문을 가진 문서가 없다는 것과 이 판정 기준이 필요 없다는 것은
다른 말이다.**

## 긴 세션에서의 지침 유실 (`project build`, `project loop`)

`build`가 한 세션에서 이슈를 연달아 처리하면 **컨텍스트가 길어질수록 지침 준수도가 무너진다.**
실측: 깨끗한 컨텍스트에서 split을 시키면 원 이슈의 `## 참조`(design-guide 링크)를 3/3 상속하지만,
실제 burndown 기록에서는 `## 참조` 상속이 **0/15**였고 서브이슈 본문이 시간순으로 581바이트 →
72바이트까지 단조 감소했다. 얇은 이슈 → 얇은 Coverage Plan → 얇은 구현 → Audit은 그 얇은 R1..Rn만
검사하므로 정상 통과한다.

그래서 `build.md` 3.5단계가 이슈마다 fresh subagent에 `build-issue.md`를 위임한다. **이 위임을
"작은 이슈니까"로 우회 가능하게 만들지 마라** — 본문이 72바이트로 줄어든 이슈들도 그때는 작아 보였다.

**커밋은 `build-issue`(subagent), 리뷰·PR은 `build`의 9.5단계다.** 원래는 커밋까지 뒤로 몰아 뒀는데
한 커밋이 비대해졌다. 옮겨도 안전한 근거는 하나다: **리뷰 대상이 `git diff <base>...HEAD`라서 미리
커밋해도 범위가 줄지 않는다.** **줄어드는 것은 커밋 크기뿐이고 리뷰 겹은 그대로다.** 이 근거가
깨지면(리뷰가 워킹 트리만 보게 바뀌면) 이 분리도 같이 깨진다 — "커밋했으니 리뷰 생략"으로는 절대
확장하지 마라.

**두 파일은 역할로 갈렸지 중복이 아니다.** 자격 증명·고객 협의 정책의 전문은 `build-issue.md`에
있고(구현하는 쪽이 쓴다), `build.md`에는 선정에 필요한 요약과 리뷰 판정 정책이 있다.
Rationalization Table도 15(오케스트레이터)/8(`build-issue`)로 갈라 나눠 뒀다 — **같은 행을 양쪽에
두지 마라.**

**`loop`도 같은 처방을 쓰되 `gap`·`persona-test`에만 쓴다** — 라운드마다 fresh subagent에 위임하고,
`build`는 인라인으로 남긴다(관측성. 위 "위임을 막는 두 번째 제약" 참조). gap은 이미 `gap.md` 2단계에서
분석을 자기 subagent에 위임하므로, loop이 gap을 감싸면 **중첩 dispatch**가 된다 — `gap.md` 2단계에 "이미
fresh subagent면 내부 dispatch 생략" 분기를 두어 이를 막았다. 이 위임을 "가벼운 라운드니까"로 우회
가능하게 만들지 마라 — build에서 반증된 판단이다.
